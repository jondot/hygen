import generatorResolver from '/home/scottp/code/workspace/hygen/src/v5/resolvers/generator'

describe('generatorResolver(hygenConfig)', () => {
  it('should be a function', () => {
    expect(typeof generatorResolver).toEqual('function')
  });
});
