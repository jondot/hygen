import paramsResolver from '/home/scottp/code/workspace/hygen/src/v5/resolvers/params'

describe('paramsResolver(hygenConfig)', () => {
  it('should be a function', () => {
    expect(typeof paramsResolver).toEqual('function')
  });
});
