import { createResolverChain, chainResolver } from '../chainPromise'

describe('chainResolver(chain)', () => {
  it('should be a function', () => {
    expect(pathTo).toBeFunction()
  })
})

describe("createResolverChain(resolvers)", () => {

});
