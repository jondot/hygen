import templatesResolver from '/home/scottp/code/workspace/hygen/src/v5/resolvers/templates'

describe('templatesResolver(hygenConfig)', () => {
  it('should be a function', () => {
    expect(typeof templatesResolver).toEqual('function')
  });
});
