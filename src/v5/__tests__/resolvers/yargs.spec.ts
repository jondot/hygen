import yargsResolver from '/home/scottp/code/workspace/hygen/src/v5/resolvers/yargs'

describe('yargsResolver(hygenConfig)', () => {
  it('should be a function', () => {
    expect(typeof yargsResolver).toEqual('function')
  });
});
