import directivesResolver from '/home/scottp/code/workspace/hygen/src/v5/resolvers/directives'

describe('directivesResolver(hygenConfig)', () => {
  it('should be a function', () => {
    expect(typeof directivesResolver).toEqual('function')
  });
});
