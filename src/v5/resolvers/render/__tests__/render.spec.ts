import renderResolver from '/home/scottp/code/workspace/hygen/src/v5/resolvers/render'

describe('renderResolver(hygenConfig)', () => {
  it('should be a function', () => {
    expect(typeof renderResolver).toEqual('function')
  });
});
