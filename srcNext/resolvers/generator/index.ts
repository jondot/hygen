import { HygenResolver } from '../../types'

export const resolveGenerator: HygenResolver = config => {
  // find and load prompt and params files
  // find and load templates
  // handle subactions

  return Promise.resolve(config)
}

